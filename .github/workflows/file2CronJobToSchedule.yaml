name: Crob Job Schedule
on:
  schedule:
    - cron: '* * * * *'

jobs:
  runCronJob:
    runs-on: ubuntu-latest

    steps:
      - name: Run Cron Scheduled time
        run: echo 'Run Cron Timing...'
